name: CI

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - '**'

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: 1. Test
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      checks: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        env:
          PIP_NEXUS_CONFIG: ${{ secrets.PIP_NEXUS_CONFIG }}
        run: |
          python -m pip install -U pip
          if [ -n "${PIP_NEXUS_CONFIG:-}" ]; then
            printf '%s\n' "$PIP_NEXUS_CONFIG" > "$RUNNER_TEMP/pip.conf"
            export PIP_CONFIG_FILE="$RUNNER_TEMP/pip.conf"
          fi
          python -m pip install -r requirements.txt
          python -m pip install -r ./requirements/requirements.test.txt

      - name: Test with pytest
        run: python -m pytest --junitxml test-results.xml

      - name: Publish test report
        if: ${{ always() }}
        uses: dorny/test-reporter@v2
        with:
          name: pytest
          path: test-results.xml
          reporter: java-junit
          fail-on-error: false

  sonar:
    name: 1.1. Sonar (master)
    runs-on: ubuntu-24.04
    needs: test
    if: ${{ github.event_name != 'pull_request' && github.ref_name == 'master' }}
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Check sonar-project.properties
        id: sonar_props
        run: |
          if [ -f sonar-project.properties ]; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: SonarQube Scan
        if: ${{ steps.sonar_props.outputs.exists == 'true' }}
        uses: SonarSource/sonarqube-scan-action@v5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

  build_publish:
    name: 2. Build and Publish
    runs-on: ubuntu-24.04
    needs: test
    if: ${{ github.event_name != 'pull_request' && (github.ref_name == 'develop' || github.ref_name == 'master') }}
    permissions:
      contents: read
    env:
      REGISTRY_HOST: nexus.dada-tuda.ru
      BACKEND_IMAGE_REPO: ${{ vars.NEXUS_DOCKER_REPOSITORY_BACKEND || 'backend' }}
      FRONTEND_IMAGE_REPO: ${{ vars.NEXUS_DOCKER_REPOSITORY_FRONTEND || 'frontend' }}
    outputs:
      image_tag: ${{ steps.image_tag.outputs.value }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Compute image tag
        id: image_tag
        run: echo "value=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"

      - name: Docker metadata (backend)
        id: meta_backend
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY_HOST }}/dada/${{ env.BACKEND_IMAGE_REPO }}
          tags: |
            type=raw,value=latest
            type=sha,format=short
            type=ref,event=branch

      - name: Docker metadata (frontend)
        id: meta_frontend
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY_HOST }}/dada/${{ env.FRONTEND_IMAGE_REPO }}
          tags: |
            type=raw,value=latest
            type=sha,format=short
            type=ref,event=branch

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
        with:
          image: tonistiigi/binfmt:qemu-v7.0.0-28

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Nexus Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY_HOST }}
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./backend/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta_backend.outputs.tags }}
          labels: ${{ steps.meta_backend.outputs.labels }}
          cache-from: type=registry,ref=${{ env.REGISTRY_HOST }}/dada/${{ env.BACKEND_IMAGE_REPO }}:cache-latest
          cache-to: type=registry,ref=${{ env.REGISTRY_HOST }}/dada/${{ env.BACKEND_IMAGE_REPO }}:cache-latest,mode=max

      - name: Build and push frontend image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./frontend/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta_frontend.outputs.tags }}
          labels: ${{ steps.meta_frontend.outputs.labels }}
          cache-from: type=registry,ref=${{ env.REGISTRY_HOST }}/dada/${{ env.FRONTEND_IMAGE_REPO }}:cache-latest
          cache-to: type=registry,ref=${{ env.REGISTRY_HOST }}/dada/${{ env.FRONTEND_IMAGE_REPO }}:cache-latest,mode=max
          build-args: |
            VITE_API_URL=${{ vars.FRONTEND_VITE_API_URL || 'https://api.example.com' }}

  update_argo_repo:
    name: 3. Update Argo Repo
    runs-on: ubuntu-24.04
    needs: build_publish
    if: ${{ github.event_name != 'pull_request' && (github.ref_name == 'develop' || github.ref_name == 'master') }}
    permissions:
      contents: read
    env:
      BACKEND_IMAGE_REPO: ${{ vars.NEXUS_DOCKER_REPOSITORY_BACKEND || 'backend' }}
      FRONTEND_IMAGE_REPO: ${{ vars.NEXUS_DOCKER_REPOSITORY_FRONTEND || 'frontend' }}
    steps:
      - name: Update image tag in Argo repo
        env:
          ARGO_REPO_URL: ${{ secrets.ARGO_REPO_URL }}
          ARGO_REPO_USERNAME: ${{ secrets.ARGO_REPO_USERNAME }}
          ARGO_REPO_PASSWORD: ${{ secrets.ARGO_REPO_PASSWORD }}
          IMAGE_TAG: ${{ needs.build_publish.outputs.image_tag }}
        run: |
          set -euo pipefail
          if [[ "${ARGO_REPO_URL}" != https://* ]]; then
            echo "ARGO_REPO_URL must be HTTPS when using git credentials"
            exit 1
          fi
          if [ -z "${ARGO_REPO_USERNAME}" ] || [ -z "${ARGO_REPO_PASSWORD}" ]; then
            echo "ARGO_REPO_USERNAME/ARGO_REPO_PASSWORD are required"
            exit 1
          fi
          CLEAN_REPO_URL="https://${ARGO_REPO_URL#https://}"

          cat > /tmp/git-askpass.sh <<'ASKPASS'
          #!/usr/bin/env bash
          case "$1" in
            *Username*) echo "$ARGO_REPO_USERNAME" ;;
            *Password*) echo "$ARGO_REPO_PASSWORD" ;;
            *) echo "" ;;
          esac
          ASKPASS
          chmod 700 /tmp/git-askpass.sh
          export GIT_ASKPASS=/tmp/git-askpass.sh
          export GIT_TERMINAL_PROMPT=0

          if [ "${GITHUB_REF_NAME}" = "master" ]; then
            BACKEND_VALUES_PATH="helm/python/services/${BACKEND_IMAGE_REPO}/values-prod.yaml"
            FRONTEND_VALUES_PATH="helm/python/services/${FRONTEND_IMAGE_REPO}/values-prod.yaml"
          else
            BACKEND_VALUES_PATH="helm/python/services/${BACKEND_IMAGE_REPO}/values-dev.yaml"
            FRONTEND_VALUES_PATH="helm/python/services/${FRONTEND_IMAGE_REPO}/values-dev.yaml"
          fi

          rm -rf /tmp/argo-repo
          git clone "${CLEAN_REPO_URL}" /tmp/argo-repo
          cd /tmp/argo-repo

          if [ ! -f "${BACKEND_VALUES_PATH}" ]; then
            echo "Values file not found: ${BACKEND_VALUES_PATH}"
            exit 1
          fi

          if [ ! -f "${FRONTEND_VALUES_PATH}" ]; then
            echo "Values file not found: ${FRONTEND_VALUES_PATH}"
            exit 1
          fi

          if grep -qE '^[[:space:]]*tag:' "${BACKEND_VALUES_PATH}"; then
            sed -Ei "s#^([[:space:]]*tag:[[:space:]]*).*\$#\\1${IMAGE_TAG}#g" "${BACKEND_VALUES_PATH}"
          else
            printf '\nimage:\n  tag: %s\n' "${IMAGE_TAG}" >> "${BACKEND_VALUES_PATH}"
          fi

          if grep -qE '^[[:space:]]*tag:' "${FRONTEND_VALUES_PATH}"; then
            sed -Ei "s#^([[:space:]]*tag:[[:space:]]*).*\$#\\1${IMAGE_TAG}#g" "${FRONTEND_VALUES_PATH}"
          else
            printf '\nimage:\n  tag: %s\n' "${IMAGE_TAG}" >> "${FRONTEND_VALUES_PATH}"
          fi

          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add "${BACKEND_VALUES_PATH}" "${FRONTEND_VALUES_PATH}"
          if ! git diff --staged --quiet; then
            git commit -m "ci: update backend/frontend image tags to ${IMAGE_TAG}"
            git push origin HEAD
          fi
